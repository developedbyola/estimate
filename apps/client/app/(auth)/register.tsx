import React from 'react';
import { Auth } from '@/features/auth';

const RegisterPage = () => {
  return (
    <React.Fragment>
      <Auth.Register />
    </React.Fragment>
  );
};

export default RegisterPage;
