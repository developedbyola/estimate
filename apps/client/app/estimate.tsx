import { Estimates } from '@/features/estimates';
import React from 'react';

const EstimatePage = () => {
  return (
    <React.Fragment>
      <Estimates.Add />
    </React.Fragment>
  );
};

export default EstimatePage;
